# 🚀 自动化工具生成器完整流程

本文档描述使用 `auto-tool-generator.js` 生成新翻译工具页面的完整工作流程。

## 📋 目录
1. [前置准备](#前置准备)
2. [自动化生成流程](#自动化生成流程)
3. [手动补充步骤](#手动补充步骤)
4. [质量检查清单](#质量检查清单)
5. [常见问题解决](#常见问题解决)

---

## 前置准备

### 1. 环境配置
确保以下环境变量已配置：
```bash
# .env.local
OPENAI_API_KEY=your_api_key_here
RESEARCH_MODEL=o3-mini  # 可选: o3-mini, o3, gpt-4o
CONTENT_MODEL=gpt-4o    # 可选: gpt-4o, gpt-4o-mini
```

### 2. 确认关键词
明确要生成的工具关键词，例如：
- `"creole to english translator"`
- `"spanish to english translator"`
- `"french text generator"`

### 3. 启动开发服务器
```bash
pnpm dev
```

---

## 自动化生成流程

### Phase 0: 复制模板页面
```bash
node scripts/auto-tool-generator.js "your keyword here"
```

**自动完成：**
- ✅ 从 `albanian-to-english` 复制页面文件
- ✅ 复制 API 路由
- ✅ 创建空的翻译文件结构

**输出位置：**
- 页面：`src/app/[locale]/(marketing)/(pages)/[slug]/`
- API：`src/app/api/[slug]/`
- 翻译：`messages/pages/[slug]/en.json`

### Phase 1: 产品调研 (o3-mini)
**自动完成：**
- 分析 Google 排名前 15 的竞品
- 收集 Quora/Reddit 上的用户讨论
- 提取 Fun Facts
- 识别市场空白功能

**输出文件：**
- `.tool-generation/[slug]/research-raw.txt`

### Phase 2: 内容调研 (o3-mini)
**自动完成：**
- 分析未覆盖的用户关心话题
- 提取高频关键词（前 30 个）
- 收集更多 Fun Facts

**输出文件：**
- `.tool-generation/[slug]/content-research.txt`

### Phase 3: 代码生成
**已被 Phase 0 替代，跳过。**

### Phase 4: 内容生成 (gpt-4o)
**自动完成：**
- SEO Title & Meta Description
- H1 标题
- Hero 描述 (20-30 词)
- What Is 板块 (60 词)
- 6 个翻译案例
- How to 使用步骤
- 2 个 Fun Facts (30 词each)
- 4 个用户感兴趣内容 (50 词each)
- 4 个产品亮点
- 6 个用户评价
- 6 个 FAQ
- CTA 文案

**字数要求标准：**
| Section | 字数要求 | 注意事项 |
|---------|---------|---------|
| Hero Description | 20-30 词 | 简要说明功能和价值 |
| What Is | 60 词 | 扩展解释功能和场景 |
| Fun Facts | 30 词/each | 有趣易懂，紧密相关 |
| User Interest | 50 词/each | 切入用户关注点 |
| Testimonials | 50-60 词/each | 真实场景，情感叙述 |

**输出文件：**
- `.tool-generation/[slug]/content-raw.txt` (原始响应)
- `.tool-generation/[slug]/content.json` (结构化内容)

### Phase 5: 生成翻译文件
**自动完成：**
- 生成 `en.json` 英文翻译
- 提示手动翻译 `zh.json`

**输出文件：**
- `messages/pages/[slug]/en.json` ✅
- `messages/pages/[slug]/zh.json` ⚠️ 需要手动翻译

### Phase 6: SEO 配置 - 自动化 ✅
**自动完成：**
- ✅ 更新 `src/app/sitemap.ts`
- ✅ 更新 `src/routes.ts`
- ✅ 更新 `src/i18n/messages.ts`

**完全自动化，无需手动操作！**

### Phase 7: 质量检查和构建验证 - 自动化 ✅
**自动完成：**
- ✅ 检查页面文件、API路由、翻译文件是否存在
- ✅ 运行 `pnpm build` 验证构建成功

**完全自动化，无需手动操作！**

### Phase 8: 图片生成 - 提示用户 📸
**需要手动操作：**
- ⚠️ 使用 Article Illustrator 生成 7 张插图
- ⚠️ 使用 Playwright 截图生成 How to 图片

**输出位置：**
- `public/images/docs/`

---

## 手动补充步骤

### 1. 翻译文件 ⚠️
**文件：** `messages/pages/[slug]/zh.json`

手动翻译中文版本（可选）

### 2. 生成图片 ⚠️

#### 2.1 使用 Article Illustrator 生成 7 张插图
**需要手动运行脚本**

生成以下图片并保存到 `public/images/docs/`：
1. `what-is-[slug].webp` - What Is 板块
2. `[slug]-fact-1.webp` - Fun Fact #1
3. `[slug]-fact-2.webp` - Fun Fact #2
4. `[slug]-interest-1.webp` - User Interest #1
5. `[slug]-interest-2.webp` - User Interest #2
6. `[slug]-interest-3.webp` - User Interest #3
7. `[slug]-interest-4.webp` - User Interest #4

#### 2.2 使用 Playwright 截图生成 How to 图片

**步骤：**
1. 创建截图脚本：`scripts/capture-[slug]-screenshot.ts`
2. 参考模板：`scripts/capture-screenshot.ts`
3. 修改 URL 和输出路径
4. 运行：`npx tsx scripts/capture-[slug]-screenshot.ts`

**输出：** `public/images/docs/[slug]-how-to.webp` (约 90KB)

**裁剪参数：**
- 左右各裁剪 150px
- 下方裁剪 100px
- 原图尺寸：1920x1080
- 输出尺寸：1620x980

---

## 质量检查清单

### 📝 第一阶段：内容质量验证

#### 1.1 字数验证（自动化）
使用 data-scientist agent 验证所有关键内容的字数：

```bash
# 手动验证命令示例
# 让 Claude 数字数并验证
```

**验证标准：**
| Section | 字数要求 | 验证方法 |
|---------|---------|---------|
| Hero Description | 20-30 词 | 使用 data-scientist agent 统计 |
| What Is | 60 词 | 使用 data-scientist agent 统计 |
| Fun Facts (each) | 30 词 | 使用 data-scientist agent 统计 |
| User Interest (each) | 50 词 | 使用 data-scientist agent 统计 |
| Testimonials (each) | 50-60 词 | 使用 data-scientist agent 统计 |

**检查项：**
- [ ] Hero Description 字数在 20-30 之间
- [ ] What Is 字数在 58-62 之间（±2 词容差）
- [ ] 2 个 Fun Facts 各自 28-32 词
- [ ] 4 个 User Interest 各自 48-52 词
- [ ] 6 个 Testimonials 各自 50-60 词

#### 1.2 JSON 字段匹配
验证所有翻译键在代码和 JSON 中正确对应：

**检查项：**
- [ ] 打开页面 `http://localhost:3000/[slug]`
- [ ] 检查页面没有显示 JSON 路径字符串（如 `"XxxPage.section.item"`）
- [ ] 验证所有文本内容正常显示（不是翻译键）
- [ ] 特别检查 Testimonials 的 heading 字段（如果存在）

**关键验证点：**
```typescript
// messages/pages/[slug]/en.json 中的所有键都应该在页面组件中被正确引用
CreoleToEnglishPage.hero.description
CreoleToEnglishPage.whatIs.description
CreoleToEnglishPage.funFacts.items[0].description
CreoleToEnglishPage.userInterest.items[0].description
CreoleToEnglishPage.testimonials.items.item-1.content
```

#### 1.3 品牌和内容质量
- [ ] **品牌名称**：VibeTrans 在以下位置出现：
  - Hero Description
  - What Is Section
  - 至少 2 个 User Interest
  - 至少 2 个 Testimonials
- [ ] **无拼写错误**：使用拼写检查工具检查英文内容
- [ ] **语法正确**：检查句子结构和时态
- [ ] **语气一致**：保持对话式、友好的语气
- [ ] **无重复内容**：各个 section 内容不重复

---

### 🔧 第二阶段：功能测试

#### 2.1 路由和导航
**基础路由测试：**
- [ ] 访问 `http://localhost:3000/[slug]`，页面正常加载
- [ ] 访问 `http://localhost:3000/en/[slug]`，英文版正常
- [ ] 访问 `http://localhost:3000/zh/[slug]`，中文版正常（如果已翻译）
- [ ] 页面标题（浏览器 tab）显示正确

**导航测试：**
- [ ] Navbar 中 "Language Translator" 下拉菜单包含新页面
- [ ] 点击 navbar 链接能正确跳转
- [ ] Footer 中 "Tools" 或相关区域包含新页面链接
- [ ] Footer 链接能正确跳转
- [ ] 页面内 CTA 按钮（"Try Now"）能正确定位到工具区域

#### 2.2 翻译工具功能
**输入测试：**
- [ ] 文本输入框可正常输入
- [ ] 输入框显示正确的 placeholder 文本
- [ ] 字符计数器（如果有）正常工作
- [ ] 清空按钮（如果有）正常工作

**翻译测试：**
- [ ] 点击 "Translate" 按钮能触发翻译
- [ ] Loading 状态显示正常
- [ ] 翻译结果正确显示在输出区域
- [ ] 错误处理正常（输入为空、API 错误等）
- [ ] 翻译结果可以复制

**文件上传测试（如果支持）：**
- [ ] 上传按钮可点击
- [ ] 支持的文件格式（.txt, .docx）可正常上传
- [ ] 不支持的文件格式显示错误提示
- [ ] 上传大文件有适当提示

#### 2.3 图片加载验证
验证所有 8 张图片正确显示：

**检查清单：**
- [ ] What Is Section: `what-is-[slug].webp` 显示正常
- [ ] How to Section: `[slug]-how-to.webp` 显示正常
- [ ] Fun Fact 1: `[slug]-fact-1.webp` 显示正常
- [ ] Fun Fact 2: `[slug]-fact-2.webp` 显示正常
- [ ] User Interest 1: `[slug]-interest-1.webp` 显示正常
- [ ] User Interest 2: `[slug]-interest-2.webp` 显示正常
- [ ] User Interest 3: `[slug]-interest-3.webp` 显示正常
- [ ] User Interest 4: `[slug]-interest-4.webp` 显示正常

**验证方法：**
```bash
# 检查所有图片是否存在
ls -lh public/images/docs/ | grep [slug]

# 检查图片尺寸和大小
# what-is: 应该是 AI 生成的插图
# how-to: 应该是 1620x980 截图（约 40-50KB）
# fact-1/2 和 interest-1/2/3/4: AI 生成的插图
```

**图片质量检查：**
- [ ] 所有图片加载速度正常（< 2s）
- [ ] 图片清晰，无模糊或压缩过度
- [ ] 图片与文本内容相关
- [ ] 图片 alt 文本正确（用于 SEO 和无障碍）

#### 2.4 响应式测试
**桌面端测试（1920x1080）：**
- [ ] 布局正常，无内容溢出
- [ ] 图片尺寸和位置合理
- [ ] 字体大小易读
- [ ] 翻译工具输入输出区域布局合理

**平板端测试（768x1024）：**
- [ ] 布局自适应，无横向滚动
- [ ] 图片缩放正常
- [ ] Navbar 变为汉堡菜单（如适用）
- [ ] 翻译工具在平板上易用

**移动端测试（375x667）：**
- [ ] 布局纵向排列，无内容重叠
- [ ] 图片宽度自适应
- [ ] 按钮大小适合触摸操作（最小 44x44px）
- [ ] 字体大小在移动端易读
- [ ] 翻译工具输入区域在移动端易用
- [ ] Testimonials 在移动端只显示 1 列

**测试工具：**
- Chrome DevTools 设备模拟器
- 或实际设备测试

---

### 🎨 第三阶段：SEO 和性能优化

#### 3.1 Meta 标签验证
在浏览器中查看页面源代码：

```html
<!-- 应该包含以下 meta 标签 -->
<title>Effortless Creole to English Translation Tool</title>
<meta name="description" content="Transform Creole to English with ease..." />
<meta property="og:title" content="..." />
<meta property="og:description" content="..." />
<meta property="og:image" content="/images/docs/what-is-[slug].webp" />
<meta name="twitter:card" content="summary_large_image" />
```

**检查项：**
- [ ] `<title>` 标签存在且长度 50-60 字符
- [ ] `<meta name="description">` 存在且长度 150-160 字符
- [ ] Open Graph 标签完整（og:title, og:description, og:image）
- [ ] Twitter Card 标签完整
- [ ] OG Image 使用 `what-is-[slug].webp`

#### 3.2 Sitemap 和结构化数据
- [ ] **Sitemap 更新**：检查 `http://localhost:3000/sitemap.xml`
  - 新页面 URL 出现在 sitemap 中（✅ 自动化完成）
  - `lastModified` 日期正确
  - `changeFrequency` 设为 "weekly"
  - `priority` 设为 1
- [ ] **内部链接**：页面内有指向其他相关工具的链接
- [ ] **结构化数据**（如果有）：Schema.org 标记正确

#### 3.3 性能测试
使用 Chrome DevTools Lighthouse 或 PageSpeed Insights：

**检查项：**
- [ ] **Performance Score > 90**
- [ ] **First Contentful Paint < 1.5s**
- [ ] **Largest Contentful Paint < 2.5s**
- [ ] **Cumulative Layout Shift < 0.1**
- [ ] **Time to Interactive < 3s**

**优化建议：**
- 图片使用 webp 格式（已实施）
- 图片尺寸优化（约 50-100KB）
- 使用 Next.js Image 组件懒加载
- 避免阻塞渲染的 JS/CSS

#### 3.4 无障碍性测试
使用 Chrome DevTools Lighthouse Accessibility 报告：

**检查项：**
- [ ] **Accessibility Score > 95**
- [ ] 所有图片有 alt 文本
- [ ] 表单元素有适当的 label
- [ ] 按钮和链接有清晰的文本或 aria-label
- [ ] 颜色对比度符合 WCAG AA 标准（至少 4.5:1）
- [ ] 键盘导航正常工作（Tab 键可以遍历所有交互元素）
- [ ] 屏幕阅读器友好（测试 NVDA 或 VoiceOver）

---

### 🛠️ 第四阶段：技术验证

#### 4.1 i18n 配置检查（✅ 自动化完成）
**文件：** `src/i18n/messages.ts`

**检查项：**
- [x] 页面翻译文件已导入（自动化完成）
- [x] Promise.all 中包含导入语句（自动化完成）
- [x] deepmerge.all 中包含合并语句（自动化完成）
- [ ] 中文翻译文件存在（如果需要多语言）：
  `messages/pages/[slug]/zh.json`

#### 4.2 TypeScript 类型检查
```bash
# 运行 TypeScript 编译器检查
npx tsc --noEmit
```

**检查项：**
- [ ] 无 TypeScript 错误
- [ ] 所有导入路径正确
- [ ] 类型定义完整（特别是 namespace 相关）

#### 4.3 构建测试（✅ 自动化完成）
```bash
# 清理并构建
rm -rf .next
pnpm build
```

**检查项：**
- [x] 构建过程无错误（自动化验证）
- [x] 构建过程无警告（或只有预期的警告）
- [x] 构建产物生成在 `.next/` 目录
- [x] 构建时间合理（< 2 分钟）

**常见构建错误：**
- JSON 格式错误（缺少逗号、引号）
- 翻译键不匹配
- 图片路径错误
- TypeScript 类型错误

#### 4.4 开发环境检查
启动开发服务器并检查控制台：

```bash
pnpm dev
```

**检查项：**
- [ ] 无 Console 错误
- [ ] 无 React 警告（key, hydration 等）
- [ ] 无 next-intl 错误（MISSING_MESSAGE 在循环检查中是正常的）
- [ ] Hot reload 正常工作

---

### 🚀 第五阶段：上线前最终检查

#### 5.1 生产环境测试
```bash
# 构建生产版本
pnpm build

# 启动生产服务器
pnpm start
```

**检查项：**
- [ ] 生产环境页面正常访问
- [ ] 所有功能在生产环境工作正常
- [ ] 无生产环境特有的错误
- [ ] API 调用正常（如果有外部 API）

#### 5.2 跨浏览器测试
**测试浏览器：**
- [ ] Chrome（最新版）
- [ ] Firefox（最新版）
- [ ] Safari（最新版，Mac/iOS）
- [ ] Edge（最新版）

**检查项：**
- [ ] 页面在所有浏览器正常显示
- [ ] 翻译功能在所有浏览器正常工作
- [ ] CSS 样式在所有浏览器一致（或有适当的降级）

#### 5.3 上线检查清单
- [ ] **代码审查**：所有代码符合项目规范
- [ ] **测试完成**：上述所有测试项通过
- [ ] **文档更新**：README 或相关文档已更新
- [ ] **Git 提交**：
  ```bash
  git add .
  git commit -m "feat: add [tool name] page with complete content and images"
  git push
  ```
- [ ] **部署验证**：部署后在生产环境验证所有功能

#### 5.4 监控和跟踪
上线后的后续跟踪：

**第一周监控：**
- [ ] Google Search Console 提交新 URL
- [ ] 监控 Google Analytics 流量
- [ ] 检查错误日志（Sentry 或类似工具）
- [ ] 收集用户反馈

**SEO 跟踪：**
- [ ] 检查页面是否被 Google 索引（site:yourdomain.com/[slug]）
- [ ] 监控关键词排名
- [ ] 分析用户搜索行为（Search Console）

---

### 📊 测试报告模板

完成测试后，生成一份测试报告：

```markdown
# [Tool Name] 页面测试报告

**测试日期：** 2025-10-12
**测试人员：** Jason Chen
**页面 URL：** /[slug]

## 测试结果总览
- ✅ 内容质量：通过
- ✅ 功能测试：通过
- ✅ SEO 优化：通过
- ✅ 技术验证：通过
- ✅ 上线准备：通过

## 详细测试结果

### 内容质量（10/10）
- Hero: 21 词 ✅
- What Is: 60 词 ✅
- Fun Facts: 28, 28 词 ✅
- User Interest: 50, 50, 50, 50 词 ✅

### 功能测试（5/5）
- 路由导航 ✅
- 翻译功能 ✅
- 图片加载 ✅
- 响应式布局 ✅

### 性能指标
- Lighthouse Performance: 95
- First Contentful Paint: 1.2s
- Largest Contentful Paint: 2.1s
- Accessibility: 98

### 问题和修复
1. ~~Testimonials heading 显示 JSON 路径~~ - 已修复

## 结论
页面已准备好上线，所有测试项通过。

## 下一步行动
- [ ] 部署到生产环境
- [ ] 提交 Google Search Console
- [ ] 监控首周流量
```

---

### 🔄 持续改进

#### 测试自动化建议
1. **单元测试**：为翻译工具组件编写 Jest 测试
2. **E2E 测试**：使用 Playwright 编写端到端测试
3. **视觉回归测试**：使用 Percy 或 Chromatic
4. **性能监控**：集成 Lighthouse CI

#### 定期审查
每月或每季度审查页面：
- 更新过时内容
- 检查断开的链接
- 优化性能指标
- 更新 SEO 策略

---

## 常见问题解决

### 问题 1: 用户评论标题显示为 JSON 路径
**症状：** `"CreoleToEnglishPage.testimonials.items.item-2.heading"`

**原因：** Testimonials 组件尝试读取不存在的 `heading` 字段

**解决方案：**
已在 `testimonials.tsx` 中修复，过滤掉未找到的翻译键：
```typescript
const headingValue = heading && !heading.includes(`items.${itemKey}`) ? heading : '';
```

### 问题 2: 内容字数不足
**症状：** 生成的内容少于要求的字数

**原因：** GPT-4o 有时不严格遵守字数要求

**解决方案：**
1. 使用 seo-content-writer agent 手动补充
2. 确保提示词中明确字数要求
3. 验证生成后的内容字数

### 问题 3: 图片未显示
**症状：** 页面上图片链接断开

**原因：** 图片文件名或路径不正确

**解决方案：**
1. 检查 `public/images/docs/` 目录
2. 确认文件名与 JSON 中的引用一致
3. 检查图片格式为 `.webp`

### 问题 4: 路由 404
**症状：** 访问 `/[slug]` 显示 404

**原因：** routes.ts 未更新或 i18n 未正确配置

**解决方案：**
1. 检查 `src/routes.ts` 是否添加路由（✅ 自动化完成）
2. 检查 `src/i18n/messages.ts` 是否正确导入（✅ 自动化完成）
3. 重启开发服务器

### 问题 5: 构建失败
**症状：** `pnpm build` 报错

**原因：** TypeScript 类型错误或 JSON 格式问题

**解决方案：**
1. 运行 `pnpm lint` 检查代码质量
2. 检查 JSON 文件格式是否正确
3. 查看具体错误信息并修复

---

## 🎯 完整工作流程时间线

| 步骤 | 预计时间 | 备注 |
|-----|---------|------|
| Phase 0-2: 自动调研 | 5-10 分钟 | 全自动 |
| Phase 4-5: 内容生成 | 3-5 分钟 | 全自动 |
| Phase 6: SEO 配置 | 自动 | ✅ 全自动 |
| Phase 7: 质量检查和构建 | 自动 | ✅ 全自动 |
| Phase 8: 图片生成 | 10-15 分钟 | 需运行脚本 |
| 手动测试 | 10 分钟 | 手动验证 |
| **总计** | **28-40 分钟** | - |

---

## 📝 优化成果总结

### ✅ 已自动化的步骤：
1. **SEO 配置（Phase 6）**
   - 自动更新 sitemap.ts
   - 自动更新 routes.ts
   - 自动更新 i18n messages.ts

2. **质量检查和构建验证（Phase 7）**
   - 自动检查文件存在性
   - 自动运行 pnpm build
   - 自动验证构建成功

### ⚠️ 仍需手动的步骤：
1. **图片生成（Phase 8）**
   - Article Illustrator 生成插图
   - Playwright 截图

2. **中文翻译（可选）**
   - 手动翻译 zh.json

---

**最后更新：** 2025-10-12
**维护者：** Jason Chen
**优化版本：** v2.0 - SEO配置和测试自动化

### Phase 0: 复制模板页面
```bash
node scripts/auto-tool-generator.js "your keyword here"
```

**自动完成：**
- ✅ 从 `albanian-to-english` 复制页面文件
- ✅ 复制 API 路由
- ✅ 创建空的翻译文件结构

**输出位置：**
- 页面：`src/app/[locale]/(marketing)/(pages)/[slug]/`
- API：`src/app/api/[slug]/`
- 翻译：`messages/pages/[slug]/en.json`

### Phase 1: 产品调研 (o3-mini)
**自动完成：**
- 分析 Google 排名前 15 的竞品
- 收集 Quora/Reddit 上的用户讨论
- 提取 Fun Facts
- 识别市场空白功能

**输出文件：**
- `.tool-generation/[slug]/research-raw.txt`

### Phase 2: 内容调研 (o3-mini)
**自动完成：**
- 分析未覆盖的用户关心话题
- 提取高频关键词（前 30 个）
- 收集更多 Fun Facts

**输出文件：**
- `.tool-generation/[slug]/content-research.txt`

### Phase 3: 代码生成
**已被 Phase 0 替代，跳过。**

### Phase 4: 内容生成 (gpt-4o)
**自动完成：**
- SEO Title & Meta Description
- H1 标题
- Hero 描述 (20-30 词)
- What Is 板块 (60 词)
- 6 个翻译案例
- How to 使用步骤
- 2 个 Fun Facts (30 词each)
- 4 个用户感兴趣内容 (50 词each)
- 4 个产品亮点
- 6 个用户评价
- 6 个 FAQ
- CTA 文案

**字数要求标准：**
| Section | 字数要求 | 注意事项 |
|---------|---------|---------|
| Hero Description | 20-30 词 | 简要说明功能和价值 |
| What Is | 60 词 | 扩展解释功能和场景 |
| Fun Facts | 30 词/each | 有趣易懂，紧密相关 |
| User Interest | 50 词/each | 切入用户关注点 |
| Testimonials | 50-60 词/each | 真实场景，情感叙述 |

**输出文件：**
- `.tool-generation/[slug]/content-raw.txt` (原始响应)
- `.tool-generation/[slug]/content.json` (结构化内容)

### Phase 5: 生成翻译文件
**自动完成：**
- 生成 `en.json` 英文翻译
- 提示手动翻译 `zh.json`

**输出文件：**
- `messages/pages/[slug]/en.json` ✅
- `messages/pages/[slug]/zh.json` ⚠️ 需要手动翻译

---

## 手动补充步骤

### 1. 更新 i18n 配置 ⚠️
**文件：** `src/i18n/messages.ts`

```typescript
// 1. 添加导入
import creoleToEnglishTranslatorPages from '../../messages/pages/creole-to-english-translator/{locale}.json';

// 2. 在 Promise.all 中添加
const [
  // ... existing imports
  creoleToEnglishTranslatorPages,
] = await Promise.all([
  // ... existing imports
  import(`../../messages/pages/creole-to-english-translator/${locale}.json`),
]);

// 3. 在 deepmerge.all 中添加
return deepmerge.all([
  // ... existing
  creoleToEnglishTranslatorPages.default,
]) as Messages;
```

### 2. 配置 SEO (sitemap, navbar, footer) ⚠️

#### 2.1 添加到 Sitemap
**文件：** `src/app/sitemap.ts`

```typescript
{
  url: `${WEBSITE_HOST_URL}/creole-to-english-translator`,
  lastModified: new Date(),
  changeFrequency: 'monthly',
  priority: 0.8,
},
```

#### 2.2 添加到 Navbar
**文件：** `src/config/navbar-config.tsx`

```typescript
{
  title: t('languageTranslator.items.creoleToEnglishTranslator.title'),
  icon: <SparklesIcon className="size-4 shrink-0" />,
  href: Routes.CreoleToEnglishTranslator,
  external: false,
},
```

#### 2.3 添加到 Footer
**文件：** `src/config/footer-config.tsx`

```typescript
{
  title: 'Creole To English Translator',
  href: Routes.CreoleToEnglishTranslator,
  external: false,
},
```

#### 2.4 添加到营销翻译
**文件：** `messages/marketing/en.json`

```json
"creoleToEnglishTranslator": {
  "title": "Creole To English Translator",
  "description": "Translate between Haitian Creole and English language"
}
```

#### 2.5 添加路由
**文件：** `src/routes.ts`

```typescript
CreoleToEnglishTranslator = '/creole-to-english-translator',
```

### 3. 生成图片 ⚠️

#### 3.1 使用 Article Illustrator 生成 7 张插图
**需要手动运行脚本**

生成以下图片并保存到 `public/images/docs/`：
1. `what-is-[slug].webp` - What Is 板块
2. `[slug]-fact-1.webp` - Fun Fact #1
3. `[slug]-fact-2.webp` - Fun Fact #2
4. `[slug]-interest-1.webp` - User Interest #1
5. `[slug]-interest-2.webp` - User Interest #2
6. `[slug]-interest-3.webp` - User Interest #3
7. `[slug]-interest-4.webp` - User Interest #4

#### 3.2 使用 Playwright 截图生成 How to 图片

**步骤：**
1. 创建截图脚本：`scripts/capture-[slug]-screenshot.ts`
2. 参考模板：`scripts/capture-screenshot.ts`
3. 修改 URL 和输出路径
4. 运行：`npx tsx scripts/capture-[slug]-screenshot.ts`

**输出：** `public/images/docs/[slug]-how-to.webp` (约 90KB)

**裁剪参数：**
- 左右各裁剪 150px
- 下方裁剪 100px
- 原图尺寸：1920x1080
- 输出尺寸：1620x980

---

## 质量检查清单

### 📝 第一阶段：内容质量验证

#### 1.1 字数验证（自动化）
使用 data-scientist agent 验证所有关键内容的字数：

```bash
# 手动验证命令示例
# 让 Claude 数字数并验证
```

**验证标准：**
| Section | 字数要求 | 验证方法 |
|---------|---------|---------|
| Hero Description | 20-30 词 | 使用 data-scientist agent 统计 |
| What Is | 60 词 | 使用 data-scientist agent 统计 |
| Fun Facts (each) | 30 词 | 使用 data-scientist agent 统计 |
| User Interest (each) | 50 词 | 使用 data-scientist agent 统计 |
| Testimonials (each) | 50-60 词 | 使用 data-scientist agent 统计 |

**检查项：**
- [ ] Hero Description 字数在 20-30 之间
- [ ] What Is 字数在 58-62 之间（±2 词容差）
- [ ] 2 个 Fun Facts 各自 28-32 词
- [ ] 4 个 User Interest 各自 48-52 词
- [ ] 6 个 Testimonials 各自 50-60 词

#### 1.2 JSON 字段匹配
验证所有翻译键在代码和 JSON 中正确对应：

**检查项：**
- [ ] 打开页面 `http://localhost:3000/[slug]`
- [ ] 检查页面没有显示 JSON 路径字符串（如 `"XxxPage.section.item"`）
- [ ] 验证所有文本内容正常显示（不是翻译键）
- [ ] 特别检查 Testimonials 的 heading 字段（如果存在）

**关键验证点：**
```typescript
// messages/pages/[slug]/en.json 中的所有键都应该在页面组件中被正确引用
CreoleToEnglishPage.hero.description
CreoleToEnglishPage.whatIs.description
CreoleToEnglishPage.funFacts.items[0].description
CreoleToEnglishPage.userInterest.items[0].description
CreoleToEnglishPage.testimonials.items.item-1.content
```

#### 1.3 品牌和内容质量
- [ ] **品牌名称**：VibeTrans 在以下位置出现：
  - Hero Description
  - What Is Section
  - 至少 2 个 User Interest
  - 至少 2 个 Testimonials
- [ ] **无拼写错误**：使用拼写检查工具检查英文内容
- [ ] **语法正确**：检查句子结构和时态
- [ ] **语气一致**：保持对话式、友好的语气
- [ ] **无重复内容**：各个 section 内容不重复

---

### 🔧 第二阶段：功能测试

#### 2.1 路由和导航
**基础路由测试：**
- [ ] 访问 `http://localhost:3000/[slug]`，页面正常加载
- [ ] 访问 `http://localhost:3000/en/[slug]`，英文版正常
- [ ] 访问 `http://localhost:3000/zh/[slug]`，中文版正常（如果已翻译）
- [ ] 页面标题（浏览器 tab）显示正确

**导航测试：**
- [ ] Navbar 中 "Language Translator" 下拉菜单包含新页面
- [ ] 点击 navbar 链接能正确跳转
- [ ] Footer 中 "Tools" 或相关区域包含新页面链接
- [ ] Footer 链接能正确跳转
- [ ] 页面内 CTA 按钮（"Try Now"）能正确定位到工具区域

#### 2.2 翻译工具功能
**输入测试：**
- [ ] 文本输入框可正常输入
- [ ] 输入框显示正确的 placeholder 文本
- [ ] 字符计数器（如果有）正常工作
- [ ] 清空按钮（如果有）正常工作

**翻译测试：**
- [ ] 点击 "Translate" 按钮能触发翻译
- [ ] Loading 状态显示正常
- [ ] 翻译结果正确显示在输出区域
- [ ] 错误处理正常（输入为空、API 错误等）
- [ ] 翻译结果可以复制

**文件上传测试（如果支持）：**
- [ ] 上传按钮可点击
- [ ] 支持的文件格式（.txt, .docx）可正常上传
- [ ] 不支持的文件格式显示错误提示
- [ ] 上传大文件有适当提示

#### 2.3 图片加载验证
验证所有 8 张图片正确显示：

**检查清单：**
- [ ] What Is Section: `what-is-[slug].webp` 显示正常
- [ ] How to Section: `[slug]-how-to.webp` 显示正常
- [ ] Fun Fact 1: `[slug]-fact-1.webp` 显示正常
- [ ] Fun Fact 2: `[slug]-fact-2.webp` 显示正常
- [ ] User Interest 1: `[slug]-interest-1.webp` 显示正常
- [ ] User Interest 2: `[slug]-interest-2.webp` 显示正常
- [ ] User Interest 3: `[slug]-interest-3.webp` 显示正常
- [ ] User Interest 4: `[slug]-interest-4.webp` 显示正常

**验证方法：**
```bash
# 检查所有图片是否存在
ls -lh public/images/docs/ | grep [slug]

# 检查图片尺寸和大小
# what-is: 应该是 AI 生成的插图
# how-to: 应该是 1620x980 截图（约 40-50KB）
# fact-1/2 和 interest-1/2/3/4: AI 生成的插图
```

**图片质量检查：**
- [ ] 所有图片加载速度正常（< 2s）
- [ ] 图片清晰，无模糊或压缩过度
- [ ] 图片与文本内容相关
- [ ] 图片 alt 文本正确（用于 SEO 和无障碍）

#### 2.4 响应式测试
**桌面端测试（1920x1080）：**
- [ ] 布局正常，无内容溢出
- [ ] 图片尺寸和位置合理
- [ ] 字体大小易读
- [ ] 翻译工具输入输出区域布局合理

**平板端测试（768x1024）：**
- [ ] 布局自适应，无横向滚动
- [ ] 图片缩放正常
- [ ] Navbar 变为汉堡菜单（如适用）
- [ ] 翻译工具在平板上易用

**移动端测试（375x667）：**
- [ ] 布局纵向排列，无内容重叠
- [ ] 图片宽度自适应
- [ ] 按钮大小适合触摸操作（最小 44x44px）
- [ ] 字体大小在移动端易读
- [ ] 翻译工具输入区域在移动端易用
- [ ] Testimonials 在移动端只显示 1 列

**测试工具：**
- Chrome DevTools 设备模拟器
- 或实际设备测试

---

### 🎨 第三阶段：SEO 和性能优化

#### 3.1 Meta 标签验证
在浏览器中查看页面源代码：

```html
<!-- 应该包含以下 meta 标签 -->
<title>Effortless Creole to English Translation Tool</title>
<meta name="description" content="Transform Creole to English with ease..." />
<meta property="og:title" content="..." />
<meta property="og:description" content="..." />
<meta property="og:image" content="/images/docs/what-is-[slug].webp" />
<meta name="twitter:card" content="summary_large_image" />
```

**检查项：**
- [ ] `<title>` 标签存在且长度 50-60 字符
- [ ] `<meta name="description">` 存在且长度 150-160 字符
- [ ] Open Graph 标签完整（og:title, og:description, og:image）
- [ ] Twitter Card 标签完整
- [ ] OG Image 使用 `what-is-[slug].webp`

#### 3.2 Sitemap 和结构化数据
- [ ] **Sitemap 更新**：检查 `http://localhost:3000/sitemap.xml`
  - 新页面 URL 出现在 sitemap 中
  - `lastModified` 日期正确
  - `changeFrequency` 设为 "monthly"
  - `priority` 设为 0.8
- [ ] **内部链接**：页面内有指向其他相关工具的链接
- [ ] **结构化数据**（如果有）：Schema.org 标记正确

#### 3.3 性能测试
使用 Chrome DevTools Lighthouse 或 PageSpeed Insights：

**检查项：**
- [ ] **Performance Score > 90**
- [ ] **First Contentful Paint < 1.5s**
- [ ] **Largest Contentful Paint < 2.5s**
- [ ] **Cumulative Layout Shift < 0.1**
- [ ] **Time to Interactive < 3s**

**优化建议：**
- 图片使用 webp 格式（已实施）
- 图片尺寸优化（约 50-100KB）
- 使用 Next.js Image 组件懒加载
- 避免阻塞渲染的 JS/CSS

#### 3.4 无障碍性测试
使用 Chrome DevTools Lighthouse Accessibility 报告：

**检查项：**
- [ ] **Accessibility Score > 95**
- [ ] 所有图片有 alt 文本
- [ ] 表单元素有适当的 label
- [ ] 按钮和链接有清晰的文本或 aria-label
- [ ] 颜色对比度符合 WCAG AA 标准（至少 4.5:1）
- [ ] 键盘导航正常工作（Tab 键可以遍历所有交互元素）
- [ ] 屏幕阅读器友好（测试 NVDA 或 VoiceOver）

---

### 🛠️ 第四阶段：技术验证

#### 4.1 i18n 配置检查
**文件：** `src/i18n/messages.ts`

**检查项：**
- [ ] 页面翻译文件已导入：
  ```typescript
  import xxxTranslatorPages from '../../messages/pages/[slug]/{locale}.json';
  ```
- [ ] Promise.all 中包含导入语句：
  ```typescript
  const [
    // ...
    xxxTranslatorPages,
  ] = await Promise.all([
    // ...
    import(`../../messages/pages/[slug]/${locale}.json`),
  ]);
  ```
- [ ] deepmerge.all 中包含合并语句：
  ```typescript
  return deepmerge.all([
    // ...
    xxxTranslatorPages.default,
  ]) as Messages;
  ```
- [ ] 中文翻译文件存在（如果需要多语言）：
  `messages/pages/[slug]/zh.json`

#### 4.2 TypeScript 类型检查
```bash
# 运行 TypeScript 编译器检查
npx tsc --noEmit
```

**检查项：**
- [ ] 无 TypeScript 错误
- [ ] 所有导入路径正确
- [ ] 类型定义完整（特别是 namespace 相关）

#### 4.3 构建测试
```bash
# 清理并构建
rm -rf .next
pnpm build
```

**检查项：**
- [ ] 构建过程无错误
- [ ] 构建过程无警告（或只有预期的警告）
- [ ] 构建产物生成在 `.next/` 目录
- [ ] 构建时间合理（< 2 分钟）

**常见构建错误：**
- JSON 格式错误（缺少逗号、引号）
- 翻译键不匹配
- 图片路径错误
- TypeScript 类型错误

#### 4.4 开发环境检查
启动开发服务器并检查控制台：

```bash
pnpm dev
```

**检查项：**
- [ ] 无 Console 错误
- [ ] 无 React 警告（key, hydration 等）
- [ ] 无 next-intl 错误（MISSING_MESSAGE 在循环检查中是正常的）
- [ ] Hot reload 正常工作

---

### 🚀 第五阶段：上线前最终检查

#### 5.1 生产环境测试
```bash
# 构建生产版本
pnpm build

# 启动生产服务器
pnpm start
```

**检查项：**
- [ ] 生产环境页面正常访问
- [ ] 所有功能在生产环境工作正常
- [ ] 无生产环境特有的错误
- [ ] API 调用正常（如果有外部 API）

#### 5.2 跨浏览器测试
**测试浏览器：**
- [ ] Chrome（最新版）
- [ ] Firefox（最新版）
- [ ] Safari（最新版，Mac/iOS）
- [ ] Edge（最新版）

**检查项：**
- [ ] 页面在所有浏览器正常显示
- [ ] 翻译功能在所有浏览器正常工作
- [ ] CSS 样式在所有浏览器一致（或有适当的降级）

#### 5.3 上线检查清单
- [ ] **代码审查**：所有代码符合项目规范
- [ ] **测试完成**：上述所有测试项通过
- [ ] **文档更新**：README 或相关文档已更新
- [ ] **Git 提交**：
  ```bash
  git add .
  git commit -m "feat: add [tool name] page with complete content and images"
  git push
  ```
- [ ] **部署验证**：部署后在生产环境验证所有功能

#### 5.4 监控和跟踪
上线后的后续跟踪：

**第一周监控：**
- [ ] Google Search Console 提交新 URL
- [ ] 监控 Google Analytics 流量
- [ ] 检查错误日志（Sentry 或类似工具）
- [ ] 收集用户反馈

**SEO 跟踪：**
- [ ] 检查页面是否被 Google 索引（site:yourdomain.com/[slug]）
- [ ] 监控关键词排名
- [ ] 分析用户搜索行为（Search Console）

---

### 📊 测试报告模板

完成测试后，生成一份测试报告：

```markdown
# [Tool Name] 页面测试报告

**测试日期：** 2025-10-12
**测试人员：** Jason Chen
**页面 URL：** /[slug]

## 测试结果总览
- ✅ 内容质量：通过
- ✅ 功能测试：通过
- ✅ SEO 优化：通过
- ✅ 技术验证：通过
- ✅ 上线准备：通过

## 详细测试结果

### 内容质量（10/10）
- Hero: 21 词 ✅
- What Is: 60 词 ✅
- Fun Facts: 28, 28 词 ✅
- User Interest: 50, 50, 50, 50 词 ✅

### 功能测试（5/5）
- 路由导航 ✅
- 翻译功能 ✅
- 图片加载 ✅
- 响应式布局 ✅

### 性能指标
- Lighthouse Performance: 95
- First Contentful Paint: 1.2s
- Largest Contentful Paint: 2.1s
- Accessibility: 98

### 问题和修复
1. ~~Testimonials heading 显示 JSON 路径~~ - 已修复

## 结论
页面已准备好上线，所有测试项通过。

## 下一步行动
- [ ] 部署到生产环境
- [ ] 提交 Google Search Console
- [ ] 监控首周流量
```

---

### 🔄 持续改进

#### 测试自动化建议
1. **单元测试**：为翻译工具组件编写 Jest 测试
2. **E2E 测试**：使用 Playwright 编写端到端测试
3. **视觉回归测试**：使用 Percy 或 Chromatic
4. **性能监控**：集成 Lighthouse CI

#### 定期审查
每月或每季度审查页面：
- 更新过时内容
- 检查断开的链接
- 优化性能指标
- 更新 SEO 策略

---

## 常见问题解决

### 问题 1: 用户评论标题显示为 JSON 路径
**症状：** `"CreoleToEnglishPage.testimonials.items.item-2.heading"`

**原因：** Testimonials 组件尝试读取不存在的 `heading` 字段

**解决方案：**
已在 `testimonials.tsx` 中修复，过滤掉未找到的翻译键：
```typescript
const headingValue = heading && !heading.includes(`items.${itemKey}`) ? heading : '';
```

### 问题 2: 内容字数不足
**症状：** 生成的内容少于要求的字数

**原因：** GPT-4o 有时不严格遵守字数要求

**解决方案：**
1. 使用 seo-content-writer agent 手动补充
2. 确保提示词中明确字数要求
3. 验证生成后的内容字数

### 问题 3: 图片未显示
**症状：** 页面上图片链接断开

**原因：** 图片文件名或路径不正确

**解决方案：**
1. 检查 `public/images/docs/` 目录
2. 确认文件名与 JSON 中的引用一致
3. 检查图片格式为 `.webp`

### 问题 4: 路由 404
**症状：** 访问 `/[slug]` 显示 404

**原因：** routes.ts 未更新或 i18n 未正确配置

**解决方案：**
1. 检查 `src/routes.ts` 是否添加路由
2. 检查 `src/i18n/messages.ts` 是否正确导入
3. 重启开发服务器

### 问题 5: 构建失败
**症状：** `pnpm build` 报错

**原因：** TypeScript 类型错误或 JSON 格式问题

**解决方案：**
1. 运行 `pnpm lint` 检查代码质量
2. 检查 JSON 文件格式是否正确
3. 查看具体错误信息并修复

---

## 🎯 完整工作流程时间线

| 步骤 | 预计时间 | 备注 |
|-----|---------|------|
| Phase 0-2: 自动调研 | 5-10 分钟 | 全自动 |
| Phase 4-5: 内容生成 | 3-5 分钟 | 全自动 |
| 更新 i18n 配置 | 2 分钟 | 手动 |
| 配置 SEO | 5 分钟 | 手动 |
| 生成图片 | 10-15 分钟 | 需运行脚本 |
| 质量检查 | 10 分钟 | 手动测试 |
| **总计** | **35-47 分钟** | - |

---

## 📝 下一步优化建议

1. **自动化 i18n 配置**：脚本自动更新 messages.ts
2. **自动化 SEO 配置**：脚本自动更新 sitemap/navbar/footer
3. **集成图片生成**：在主流程中自动生成所有图片
4. **添加字数验证**：生成后自动检查字数
5. **创建 E2E 测试**：自动验证页面功能

---

**最后更新：** 2025-10-12
**维护者：** Jason Chen
